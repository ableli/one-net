// 定义项目用到的maven仓库
allprojects {
    repositories {
        // 先后原则：速度越快越前
        maven { url 'http://maven.aliyun.com/nexus/content/groups/public/' }
        jcenter()
        mavenCentral()
        maven { url 'http://maven.weimob.com/content/groups/public/' }
    }
}
